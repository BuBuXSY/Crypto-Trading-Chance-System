# 小虞沁XYQ终极交易系统 V2 增强版依赖包
# Enhanced Trading System Requirements

# ============= 核心依赖 (必须安装) =============
# 数据处理和数值计算
pandas>=2.0.0
numpy>=1.24.0

# 交易所接口
ccxt>=4.0.0

# 网络请求
requests>=2.31.0

# 日期时间处理
python-dateutil>=2.8.0

# JSON处理 (Python内置)
# json  # Python内置模块

# ============= 机器学习依赖 (AI功能) =============
# 基础机器学习 - 强烈推荐
scikit-learn>=1.3.0

# 梯度提升算法 - 推荐
xgboost>=1.7.0

# 深度学习框架 - 推荐 (LSTM功能)
tensorflow>=2.13.0
# 或者使用 PyTorch (二选一)
# torch>=2.0.0
# torchvision>=0.15.0

# 科学计算 - 推荐 (高级指标)
scipy>=1.11.0

# ============= 数据可视化 (可选) =============
# 如果需要本地图表展示
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# ============= 开发和调试工具 (可选) =============
# 代码格式化
black>=23.0.0
autopep8>=2.0.0

# 类型检查
mypy>=1.5.0

# 测试框架
pytest>=7.4.0
pytest-cov>=4.1.0

# 性能分析
memory-profiler>=0.61.0
line-profiler>=4.1.0

# ============= 其他可选依赖 =============
# 更快的JSON处理
orjson>=3.9.0

# 异步HTTP客户端 (如果需要异步功能)
aiohttp>=3.8.0
asyncio  # Python内置

# 数据库ORM (如果需要更复杂的数据库操作)
sqlalchemy>=2.0.0

# 配置文件处理
pyyaml>=6.0
configparser  # Python内置

# 加密和安全
cryptography>=41.0.0

# 进度条显示
tqdm>=4.65.0

# 缓存
diskcache>=5.6.0

# ============= 系统监控 (可选) =============
# 系统资源监控
psutil>=5.9.0

# 内存使用优化
pympler>=0.9

# ============= 安装说明 =============
# 
# 最小安装 (仅核心功能):
# pip install pandas numpy ccxt requests python-dateutil
#
# 推荐安装 (包含AI功能):
# pip install pandas numpy ccxt requests python-dateutil scikit-learn xgboost tensorflow scipy
#
# 完整安装:
# pip install -r requirements.txt
#
# ============= 版本兼容性说明 =============
# Python版本要求: >=3.8
# 操作系统: Windows 10+, macOS 10.14+, Linux (Ubuntu 18.04+)
#
# ============= 可选安装组合 =============
# 
# AI轻量版 (无深度学习):
# pip install scikit-learn xgboost scipy
#
# AI完整版 (包含深度学习):
# pip install scikit-learn xgboost tensorflow scipy
#
# 开发版 (包含开发工具):
# pip install black autopep8 mypy pytest pytest-cov
#
# ============= 特殊说明 =============
#
# 1. TensorFlow在某些系统上可能需要特殊安装:
#    - Windows: 确保安装Microsoft Visual C++ Redistributable
#    - macOS M1/M2: 使用 tensorflow-macos
#    - Linux: 可能需要CUDA支持 (GPU版本)
#
# 2. XGBoost在某些环境下可能需要编译:
#    - Windows: 推荐使用conda安装
#    - 或者使用预编译轮子: pip install xgboost
#
# 3. 如果遇到依赖冲突，建议使用虚拟环境:
#    python -m venv venv
#    source venv/bin/activate  # Linux/macOS
#    venv\Scripts\activate     # Windows
#    pip install -r requirements.txt